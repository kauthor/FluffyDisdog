using System;

namespace FluffyDisdog.UI.Part
{
    public class RequestResultRelicPart:RequestResultPart
    {
        public override void Init(RequestReward reward, Action<bool> onclick)
        {
            base.Init(reward, onclick);
        }
    }
}